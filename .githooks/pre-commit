#!/bin/sh
EXPECTED="dev@isquantum.money"
ACTUAL=$(git config user.email)

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "ERROR: git user.email is '$ACTUAL', must be '$EXPECTED'"
  echo "Fix: git config user.email dev@isquantum.money"
  exit 1
fi
